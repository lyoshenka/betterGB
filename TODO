* Add script to automate updates
* Minify css/js?

* Add these options
 - Hide "see all bookmarks" button
 - Hide bottom links bar
 - Hide top bar (already hidden but should be customizable)
 - Autosubmit
 - Focus submit button to start
 - Include "notes" field in tab order
 - Hide "notes" field
 - Custom color for highlighting selected tag autocomplete
 - Custom padding width
 - "minimal" option that does most of these
 - Post form on enter (no need to tab to "Submit" button)

* Add several "use types" that configure multiple options: one-click submit, simple, full (with-notes). 

* Email this out to some blogs?
* Make options page look nicer
* Add link to github page from bookmarklet screen
* Post to reddit
